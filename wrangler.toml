name = "blueos"                  # ← Must match your dashboard Worker name exactly
compatibility_date = "2026-02-01"  # or a recent date like "2026-02-15"
main = "src/index.js"            # adjust if your entrypoint is different (check repo)
# ... keep any other existing config like bindings, vars, etc.