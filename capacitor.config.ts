import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.courtreinland.kawaiichess',
  appName: 'Kawaii Chess',
  webDir: 'dist'
};

export default config;
